{
  "name": "fast-ftl",
  "version": "1.1.5",
  "description": "The Freemaker parsing service in Node.js",
  "files": [
    "dist",
    "lib/Fast-FTL.jar",
    "lib/fastjson-1.1.30.jar",
    "lib/freemarker.jar",
    "lib/json-simple-1.1.jar",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "dev": "babel src -w -d dist -s -x .js",
    "build": "babel src -d dist -s -x .js",
    "release": "npm run build && npm publish",
    "test": "node test/test.index"
  },
  "main": "./dist/index.js",
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-plugin-transform-runtime": "^6.15.0",
    "babel-preset-es2015": "^6.13.2",
    "babel-preset-stage-3": "^6.22.0"
  },
  "dependencies": {
    "babel-runtime": "^6.22.0",
    "portfinder": "^1.0.12"
  }
}
